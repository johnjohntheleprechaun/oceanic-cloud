openapi: '3.0.2'
info:
  title: Oceanic API
  version: '1.0'
components:
  securitySchemes:
    cognito:
      type: http
      scheme: bearer
      bearerFormat: JWT

paths:
  /test:
    get:
      responses:
        '200':
          description: OK
  /user/{userId}/document/{documentId}:
    get:
      summary: fetch the document info and the signed URLs needed to use the document content
      responses:
        "200":
          description: returns 