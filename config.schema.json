{
    "type": "object",
    "properties": {
        "domainName": {
            "type": "string",
            "description": "The domain name to put API gateway behind."
        },
        "certArn": {
            "type": "string",
            "description": "The AWS certificate for `domainName`"
        },
        "oAuthCallbacks": {
            "type": "array",
            "items": { "type": "string" },
            "description": "An array of authorized callback URLs for cognito oAuth."
        },
        "logoutUrls": {
            "type": "array",
            "items": { "type": "string" },
            "description": "An array of allowed signout URLs for Cognito to redirect to after a signout."
        }
    }
}